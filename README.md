# Helm chart for simple java service
In this case service resouce will act as LoadBalancer and will also do service discovery for the Pods
The deployemnt resource has HPA which will horizontally scale the infra as CPU increases above 50%
